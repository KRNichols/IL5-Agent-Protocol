# Fixture evidence (not a live host)

Used to smoke `tools/collect_neo4j_ec2_evidence.sh --local-root` and
`tools/answer_bank_from_evidence.py`. These files are **intentionally weak**
so CONFIG-CHANGES.md has rows. They are not production evidence and not a PASS.
