# Provenance pointer

Canonical provenance (official URLs + exact counts) lives at:

[`../il5-scanner/banks/PROVENANCE.md`](../il5-scanner/banks/PROVENANCE.md)

Machine copy: `SOURCE.json` (this directory and `il5-scanner/banks/`).

Default grade path is FedRAMP High / Class D, not NIST SP 800-53B HIGH.
`READY` is never ATO. High alone fails IL5. See `../KEYS.md`.
